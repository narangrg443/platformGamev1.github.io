// document.addEventListener('DOMContentLoaded', function () {
//   // Check a condition to determine if Eruda should be initialized
//   if (process.env.NODE_ENV === 'development') {
//     // Ensure eruda is available globally
//     if (window.eruda) {
//       eruda.init();
//     } else {
//       console.error('Eruda not found. Make sure it is globally installed.');
//     }
//   }
// });

eruda.init();
